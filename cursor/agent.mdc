---
description: Direct agent contract — slim rules, Uber workflow, no-mistakes shipping.
globs:
alwaysApply: true
---

Read `~/agent-config/sahab.md` for workflow preferences and `~/agent-config/pr-conventions.md` before go-code ship work.

Address sahab as **sahab** at least once per response. Ship go-code through `/no-mistakes` and `arh`. Merge only when sahab approves. No AI attribution in git or PR text. Be short and precise.
